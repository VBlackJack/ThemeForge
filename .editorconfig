root = true

[*]
indent_style = space
indent_size = 4
end_of_line = crlf
charset = utf-8
trim_trailing_whitespace = true
insert_final_newline = true

[*.{xml,xaml,csproj,props,targets,slnx}]
indent_size = 2

[*.{md,yml,yaml,json}]
indent_size = 2
trim_trailing_whitespace = false

[*.cs]
# Apache 2.0 file header required (enforced via dev-standards skill)
csharp_style_var_for_built_in_types = false:warning
csharp_style_var_when_type_is_apparent = false:warning
csharp_style_var_elsewhere = false:warning
# Make the explicit-type rule load-bearing on its own: fire IDE0008 as an error
# directly, instead of relying on the option severity being promoted via
# TreatWarningsAsErrors (which varies across SDK patch versions).
dotnet_diagnostic.IDE0008.severity = error
csharp_style_namespace_declarations = file_scoped:warning
dotnet_sort_system_directives_first = true
