<!--
  Copyright 2026 Julien Bombled
  Licensed under the Apache License, Version 2.0

  Default style and template for the ToggleSwitch composite. Pill-shaped
  track (36x20) with a circular thumb (16x16) that glides 16px between
  the Off and On positions over 150ms. Track color
  snaps between CommentBrush (Off) and AccentBrush (On); the thumb fill
  uses ForegroundBrush so it contrasts automatically against both states
  in every theme without hardcoding.
-->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:motion="clr-namespace:ThemeForge.Controls.Theming"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:composites="clr-namespace:ThemeForge.Controls.Composites">

    <Style TargetType="{x:Type composites:ToggleSwitch}">
        <Setter Property="motion:Motion.SystemAnimationsEnabled" Value="{DynamicResource {x:Static SystemParameters.ClientAreaAnimationKey}}"/>
        <Setter Property="Foreground"   Value="{DynamicResource TextPrimaryBrush}"/>
        <Setter Property="FontFamily"   Value="Segoe UI"/>
        <Setter Property="FontSize"     Value="14"/>
        <Setter Property="Cursor"       Value="Hand"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment"   Value="Center"/>
        <Setter Property="Background"   Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type composites:ToggleSwitch}">
                    <StackPanel Orientation="Horizontal"
                                Background="{TemplateBinding Background}"
                                VerticalAlignment="Center">
                        <Grid x:Name="PART_SwitchRoot"
                              Width="36"
                              Height="20">
                            <Border x:Name="PART_Track"
                                    CornerRadius="10"
                                    Background="{DynamicResource CommentBrush}"
                                    SnapsToDevicePixels="True"/>
                            <Border x:Name="PART_Thumb"
                                    Width="16"
                                    Height="16"
                                    CornerRadius="8"
                                    Background="{DynamicResource ForegroundBrush}"
                                    BorderBrush="{DynamicResource BackgroundBrush}"
                                    BorderThickness="1"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Margin="2,0,0,0"
                                    SnapsToDevicePixels="True">
                                <Border.RenderTransform>
                                    <TranslateTransform x:Name="PART_ThumbTransform" X="0"/>
                                </Border.RenderTransform>
                            </Border>
                        </Grid>
                        <ContentPresenter x:Name="contentPresenter"
                                          Content="{TemplateBinding Content}"
                                          VerticalAlignment="Center"
                                          Margin="8,0,0,0"
                                          TextElement.Foreground="{TemplateBinding Foreground}"
                                          TextElement.FontFamily="{TemplateBinding FontFamily}"
                                          TextElement.FontSize="{TemplateBinding FontSize}"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Content" Value="{x:Null}">
                            <Setter TargetName="contentPresenter" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="Content" Value="">
                            <Setter TargetName="contentPresenter" Property="Visibility" Value="Collapsed"/>
                        </Trigger>

                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="PART_Track" Property="Background" Value="{DynamicResource AccentBrush}"/>
                            <Setter TargetName="PART_Thumb" Property="RenderTransform"><Setter.Value><TranslateTransform X="16"/></Setter.Value></Setter>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="True"/>
                                <Condition Property="motion:Motion.ReduceMotion" Value="False"/>
                                <Condition Property="motion:Motion.SystemAnimationsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.EnterActions>
                                <BeginStoryboard Name="switchOn">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="PART_Thumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                                                         From="0" To="16" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.EnterActions>
                            <MultiTrigger.ExitActions><RemoveStoryboard BeginStoryboardName="switchOn"/></MultiTrigger.ExitActions>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="False"/>
                                <Condition Property="motion:Motion.ReduceMotion" Value="False"/>
                                <Condition Property="motion:Motion.SystemAnimationsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <MultiTrigger.EnterActions>
                                <BeginStoryboard Name="switchOff">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="PART_Thumb" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                                                         From="16" To="0" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </MultiTrigger.EnterActions>
                            <MultiTrigger.ExitActions><RemoveStoryboard BeginStoryboardName="switchOff"/></MultiTrigger.ExitActions>
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                            <Setter Property="Cursor"  Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
