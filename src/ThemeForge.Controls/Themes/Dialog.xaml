<!--
  Copyright 2026 Julien Bombled
  Licensed under the Apache License, Version 2.0

  Default style and template for the Dialog composite. Header / Body / Footer
  layout with optional severity accent, icon, and close affordance.
-->
<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:composites="clr-namespace:ThemeForge.Controls.Composites">

  <Style x:Key="Dialog.CloseButtonStyle" TargetType="{x:Type Button}">
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Padding" Value="0"/>
    <Setter Property="Focusable" Value="False"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Button}">
          <Border x:Name="bd"
                  Background="{TemplateBinding Background}"
                  CornerRadius="4">
            <ContentPresenter HorizontalAlignment="Center"
                              VerticalAlignment="Center"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="bd" Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="{x:Type composites:Dialog}">
    <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="20"/>
    <Setter Property="FontFamily" Value="Segoe UI"/>
    <Setter Property="HorizontalAlignment" Value="Center"/>
    <Setter Property="VerticalAlignment" Value="Center"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type composites:Dialog}">
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="8"
                  SnapsToDevicePixels="True">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="*"/>
              </Grid.ColumnDefinitions>

              <Border x:Name="AccentBar"
                      Grid.Column="0"
                      Background="Transparent"
                      CornerRadius="8,0,0,8"/>

              <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto"/>
                  <RowDefinition Height="*"/>
                  <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0"
                           Margin="{TemplateBinding Padding}"
                           LastChildFill="True">
                  <Button x:Name="PART_CloseButton"
                          DockPanel.Dock="Right"
                          Style="{StaticResource Dialog.CloseButtonStyle}"
                          Width="24"
                          Height="24">
                    <Path Data="M 0,0 L 10,10 M 10,0 L 0,10"
                          Stroke="{DynamicResource TextSecondaryBrush}"
                          StrokeThickness="1.4"
                          Width="10"
                          Height="10"
                          Stretch="Uniform"/>
                  </Button>
                  <Path x:Name="HeaderIcon"
                        DockPanel.Dock="Left"
                        Width="20"
                        Height="20"
                        Margin="0,0,8,0"
                        Data="{TemplateBinding IconGeometry}"
                        Fill="{DynamicResource AccentBrush}"
                        Stretch="Uniform"/>
                  <ContentPresenter x:Name="HeaderRegion"
                                    ContentSource="Header"
                                    VerticalAlignment="Center"
                                    TextElement.FontSize="16"
                                    TextElement.FontWeight="SemiBold"
                                    TextElement.Foreground="{DynamicResource TextPrimaryBrush}"/>
                </DockPanel>

                <Border x:Name="HeaderDivider"
                        Grid.Row="0"
                        Height="1"
                        VerticalAlignment="Bottom"
                        Background="{DynamicResource BorderBrush}"
                        Opacity="0.5"/>

                <ContentPresenter Grid.Row="1"
                                  Margin="{TemplateBinding Padding}"
                                  TextElement.Foreground="{TemplateBinding Foreground}"/>

                <Border x:Name="FooterDivider"
                        Grid.Row="2"
                        Height="1"
                        VerticalAlignment="Top"
                        Background="{DynamicResource BorderBrush}"
                        Opacity="0.5"/>

                <ContentPresenter x:Name="FooterRegion"
                                  Grid.Row="2"
                                  Content="{TemplateBinding Footer}"
                                  ContentTemplate="{TemplateBinding FooterTemplate}"
                                  Margin="{TemplateBinding Padding}"
                                  TextElement.Foreground="{DynamicResource TextSecondaryBrush}"/>
              </Grid>
            </Grid>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="Header" Value="{x:Null}">
              <Setter TargetName="HeaderRegion" Property="Visibility" Value="Collapsed"/>
              <Setter TargetName="HeaderDivider" Property="Visibility" Value="Collapsed"/>
            </Trigger>
            <Trigger Property="Footer" Value="{x:Null}">
              <Setter TargetName="FooterRegion" Property="Visibility" Value="Collapsed"/>
              <Setter TargetName="FooterDivider" Property="Visibility" Value="Collapsed"/>
            </Trigger>
            <Trigger Property="IconGeometry" Value="{x:Null}">
              <Setter TargetName="HeaderIcon" Property="Visibility" Value="Collapsed"/>
            </Trigger>
            <Trigger Property="IsClosable" Value="False">
              <Setter TargetName="PART_CloseButton" Property="Visibility" Value="Collapsed"/>
            </Trigger>
            <Trigger Property="Severity" Value="Info">
              <Setter TargetName="AccentBar" Property="Background" Value="{DynamicResource InfoBrush}"/>
            </Trigger>
            <Trigger Property="Severity" Value="Success">
              <Setter TargetName="AccentBar" Property="Background" Value="{DynamicResource SuccessBrush}"/>
            </Trigger>
            <Trigger Property="Severity" Value="Warning">
              <Setter TargetName="AccentBar" Property="Background" Value="{DynamicResource WarningBrush}"/>
            </Trigger>
            <Trigger Property="Severity" Value="Error">
              <Setter TargetName="AccentBar" Property="Background" Value="{DynamicResource ErrorBrush}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>
